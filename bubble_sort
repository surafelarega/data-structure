arr = [9,4,3,100,1,8]

for i in range(len(arr)):
  for j in range(i+1, len(arr)):
    if arr[i] > arr[j]:
      # python swaping
      # arr[i] > arr[j] = arr[j], arr[i]

      # Normal Swaping
      temp = arr[i]
      arr[i] = arr[j]
      arr[j] = temp

print(arr)